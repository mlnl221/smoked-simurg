# smoked-simurg — minimal deps (see PLAN.md §6)
# Install in isolated venv: make venv  OR  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
click>=8.0
requests>=2.28
ratelimit>=2.2
beautifulsoup4>=4.12
torf>=4.1
pypdf>=4.0
ebooklib>=0.18
pillow>=10.0
# qBittorrent push after upload (simurg/uploader/client.py, optional unless [client] enabled)
qbittorrent-api>=2025.2.0
# tomli needed only on Python <3.11 (tomllib is stdlib from 3.11+)
tomli>=2.0; python_version < "3.11"

# dev
pytest>=8.0
pytest-mock>=3.10
ruff>=0.6
pre-commit>=3.5
